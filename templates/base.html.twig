<!DOCTYPE html>
<html>
    <head>
         <link rel='shortcut icon' href='{{ asset('images/favicon.jpg')}}'/>
    	<meta name="viewport" content="width=device-width, height=device-height, initial-scale=1.0, minimum-scale=1.0">
        <meta charset="UTF-8">
        <meta name="robots" content="noindex">
        <title>{% block title %}Bienvneue !{% endblock %}</title>
        {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/normalize.css') }}">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.css">
        {% endblock %}
    </head>
    <header>
    	<div class="logo-header">
    		<a href="{{ path('home') }}">
    			<img title="Logo Résonance Communication" src='{{ asset('images/logo-resonance-2018.png') }}' / >
            </a>
    	</div>
        {% if is_granted('ROLE_ADMIN') %} 
            <div class="dashlink">
                <a href="{{ path('easyadmin') }}"> Administration
                    <i class="fa fa-tachometer-alt"></i>
                </a>
            </div>
        {% endif %}
    </header>
    <body>
    {% if app.user %}
        <a class="deconnexion" href="{{ path('app_logout') }}"><img src="{{ asset('images/logout.png')}}" /></a>
    {% endif %}
        {% block body %}{% endblock %}
    <div class="disclaimer"> 
        <p>Ces vidéos tutoriels sont à destination des clients de Résonance Communication, il n'est pas autorisé de les partager ou de les diffuser publiquement</p>
    </div>
        {% block javascripts %}{% endblock %}
    </body>
    <footer>
    	<div class='footer-wrap'>
    		<p>&copy; 2019 - Création <a target="_blank" href='http://www.resonancecommunication.com'>Résonance Communication</a><p>
    	</div>
    </footer>
</html>
